[package]
name = "bsargeom"
version = "0.1.0"
edition = "2024"

[dependencies.bevy]
version = "0.19"
default-features = false
# Fine-grained features kept on purpose: the curated collections ("3d", "3d_api",
# "common_api", ...) all pull subsystems this app doesn't use (gltf, picking, scene,
# gamepad, text/fonts, clipboard, wayland, animation) and would grow the wasm binary.
features = [
    "bevy_pbr",
    "bevy_render",
    "bevy_winit",
    "bevy_window",
    "multi_threaded",
    "tonemapping_luts",
    "sysinfo_plugin",
    "mouse",
    "keyboard",
    # "webgl2",
    "webgpu",
    "x11",
    # "zstd_rust"
]

[dependencies]
bevy_panorbit_camera = { version = "0.35", features = ["bevy_egui"] }
bevy_egui = "0.40"
egui_extras = { version = "0.34", features = ["image"] }
image = { version = "0.25", default-features = false, features = ["png"] }
plotters = "0.3"
rustc-hash = "2.1"

# Size optimization (see: https://github.com/johnthagen/min-sized-rust)
[profile.release]
strip = true # = "symbols" Automatically strip symbols from the binary.
lto = true
codegen-units = 1

[profile.web-release]
inherits = "release"
strip = false # = "none" Do not strip symbols from the binary.
opt-level = "z"
